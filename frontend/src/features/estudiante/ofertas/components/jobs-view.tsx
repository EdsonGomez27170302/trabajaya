import { PlaceholderCard } from "@/components/shared/placeholder-card";

export function JobsView() {
  return (
    <PlaceholderCard
      eyebrow="Ofertas"
      title="Explora ofertas para estudiantes"
      description="Aquí se integrará la lista, filtros, modal de detalle y postulación en la siguiente fase."
    />
  );
}
