import { PlaceholderCard } from "@/components/shared/placeholder-card";

export function LoginForm() {
  return (
    <PlaceholderCard
      eyebrow="Acceso"
      title="Inicia sesión como estudiante, empresa o administrador"
      description="La autenticación JWT y el redireccionamiento por rol se conectarán en esta vista."
    />
  );
}
